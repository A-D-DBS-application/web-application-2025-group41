<!DOCTYPE html>
<html lang="{{ session.get('lang', 'nl') }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mijn aanvragen</title>
  <link rel="stylesheet" href="{{ url_for('static', filename='css/aanvragen.css') }}">
</head>
<body>

<h1>Aperçu de vos demandes</h1>

<table id="tabel">
  <thead>
    <tr>
      <th>Date</th>
      <th>Poids total DASRI</th>
      <th>Nombre de fûts WIVA</th>
      <th>Coût actuel des fûts WIVA par an</th>
      <th>Contenu par fût WIVA</th>
      <th>Coût actuel de collecte et d'incinération des DASRI par an (sans coût des fûts WIVA)</th>
    </tr>
  </thead>
  <tbody>
    {% for req in requests %}
      {% if req.waste_profile %}
      <tr>
        <td>{{ req.created_at.strftime('%d/%m/%Y') }}</td>
        
        <td>{{ req.waste_profile.hmw_total_weight }}</td>
        <td>{{ req.waste_profile.number_of_barrels_1 }}</td>
        <td>{{ req.waste_profile.cost_hmw_barrels_1 }}</td>
        <td>{{ req.waste_profile.volume_barrels_1 }}</td>
        <td>{{ req.waste_profile.cost_collection_processing }}</td>
      </tr>
      {% endif %}
    {% else %}
      <tr>
        <td colspan="6" style="text-align:center;">Aucune demande trouvée.</td>
      </tr>
    {% endfor %}
  </tbody>
</table>

<a class="back" href="{{ url_for('main.dashboard') }}">← Retour au tableau de bord</a>

</body>
</html>
